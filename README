OData Server
===========

This is a rails plugin/engine to embue your application with OData features (so its data can be readily consumed by 
and OData client).

It was forked Mark Borkum's work at from http://rubyforge.org/scm/?group_id=9605 .
